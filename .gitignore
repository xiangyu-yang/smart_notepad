# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
dist-electron
release
out

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / OS
.DS_Store
.idea
.vscode/*
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Dev database (dev mode stores db in project root)
*.db
*.db-wal
*.db-shm
.data/

# 附件存储目录（用户上传的二进制文件，属用户数据，不入库）
attachments/

# Electron 单例运行时锁文件（每次启动自动重建，不应入库）
SingletonCookie
SingletonLock
SingletonSocket

# 临时调试脚本
test-db.cjs

# IDE 本地配置
.trae/

# 本机启动脚本（含本机绝对路径，不入库）
启动智能记事本.command
*.command

